version: '3.8'

services:
  backend:
    build: .
    container_name: yeshua-backend
    ports:
      - "3001:3001"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - DIRECT_URL=${DIRECT_URL}
    restart: unless-stopped
