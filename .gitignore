# Dependências
node_modules/

# Variáveis de ambiente (NUNCA enviar para o GitHub!)
.env
.env.local
.env.production

# Logs
*.log
error.log
out.txt

# Build / gerado pelo TypeScript
dist/
build/

# Prisma gerado localmente (será regenerado no build do Render)
# A pasta prisma/migrations pode ser enviada normalmente

# Sistema operacional
.DS_Store
Thumbs.db
